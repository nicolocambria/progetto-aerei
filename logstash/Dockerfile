# Logstash 8.12.2 ufficiale: nessun plugin extra necessario
# (http input, kafka output ed elasticsearch output sono inclusi;
# il fetch dei dati è responsabilità del collector Python).
FROM docker.elastic.co/logstash/logstash:8.12.2
